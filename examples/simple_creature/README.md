# Simple creature example

Copy `aculptoi.toml` to the project root (or pass `--config`), start compatible local model servers, then run:

```bash
aculptoi blender start
aculptoi run "create a simple creature from primitive forms"
```

V1 only provides primitive construction and voxel remeshing; this example demonstrates the feedback-loop architecture, not production character generation.
